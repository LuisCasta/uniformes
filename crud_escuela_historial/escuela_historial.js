$(document).ready(function(){

//INICIAR SESION
$('#entrar').click(function(){
  $('#titulo').text(""); 
	/*$.ajax
	({
	   url: 'colono_login/check_login.php',
	   data: {"input_usuario":correo, "input_password":password},
	   type: 'post',
	   success: function(result){		   	
	   	if(result == "exito")
	   		window.location.replace("../perfil.php");	   	
	   	else if(result == "baja")
	    	M.toast({html: "Este usuario fue suspendido, hable con el administrador"});
		else
	    	M.toast({html: result});
	   }	
	});*/
});
//CREAR
});